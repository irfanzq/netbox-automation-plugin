# MAAS vs NetBox, OpenStack vs NetBox drift and apply logic.
#
# - audit_detail, maas_netbox: drift audit / comparison helpers
# - apply_cells, branch, merge, service, views: branch reconciliation (preview, frozen ops, apply)
